services:
  - type: web
    name: livercare-ai
    env: node
    buildCommand: |
      npm install
      pip install -r requirements.txt
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PYTHON_PATH
        value: python3
    healthCheckPath: /
    plan: free

